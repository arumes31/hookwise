name: Cleanup Old GHCR Packages

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (if true, will not delete anything)'
        type: boolean
        default: true
      keep_last_n:
        description: 'Number of extra versions to keep (besides latest/main)'
        type: number
        default: 5

permissions:
  contents: read
  packages: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Delete old package versions
        uses: dataaxiom/ghcr-cleanup-action@f092b48ba3b604b2a83690dc4b2bbb3392e1045f # v1.3.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          package-name: ${{ github.event.repository.name }}
          exclude-tags: ^(main|latest)$
          keep-n-tagged: ${{ inputs.keep_last_n }}
          dry-run: ${{ inputs.dry_run }}
          # Only target container packages
          package-type: container
